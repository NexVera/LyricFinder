<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/songDetailFragment">

    <fragment
        android:id="@+id/songDetailFragment"
        android:name="com.example.lyricsfinder.presentation.song_detail.SongDetailFragment"
        android:label="fragment_song_detail"
        tools:layout="@layout/fragment_song_detail" >
        <argument
            android:name="songId"
            app:argType="string" />
    </fragment>

</navigation>